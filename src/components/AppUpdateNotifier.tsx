'use client';

import React from 'react';

/**
 * App Update Notifier - Simplified version without PWA functionality
 * Shows a manual refresh notice for regular web app updates
 */
export function AppUpdateNotifier() {
    // Since we removed service workers, this component now does nothing
    // You can manually refresh the page to get updates when deployed
    return null;
}
